# API Тестирование PetStore (Postman)

Коллекция тестов для Swagger Petstore API. Проект демонстрирует навыки тестирования REST API. Цель - продемонстрировать понимание, а не усидчивость :)

## 📁 Что внутри
- **postman/** — коллекция и окружение для импорта в Postman
- **screenshots/** — результаты выполнения тестов
- 15+ запросов

## 🧪 Протестированные сценарии
### Модуль Pet
-  POST /pet — создание питомца _(positive+negative)_
-  GET /pet/{id} — получение по ID _(positive+negative)_
-  PUT /pet — обновление данных  
-  DELETE /pet/{id} — удаление
-  GET /pet/findByStatus — поиск по статусу

### Модуль Store  
-  POST /store/order — создание заказа _(positive+negative)_
-  GET /store/order/{id} — получение заказа _(positive+negative)_
-  DELETE /store/order/{id} — удаление заказа

### Модуль User
-  POST /user — создание пользователя
-  GET /user/{username} — получение пользователя
-  PUT /user/{username} — обновление
-  DELETE /user/{username} — удаление

## 📸 Результаты (что влезло)
![Результаты прогона](screenshots/run_results.png)

## 🚀 Как запустить
1. Импортируйте `postman/PetStore_Collection.json` в Postman
2. Запустите Collection Runner
3. *(Опционально)* Переменная `{{baseURL}}` имеет значение - `https://petstore.swagger.io`. Если она по какой-то причине не импортировалась.

## 🎯 Демонстрируемые навыки
- Тестирование REST API (CRUD операции)
- Работа с Postman (коллекции, переменные, окружения)
- Негативное тестирование
- Документирование тестов